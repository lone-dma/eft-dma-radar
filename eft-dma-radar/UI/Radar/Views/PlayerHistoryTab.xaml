<UserControl x:Class="eft_dma_radar.UI.Radar.Views.PlayerHistoryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:eft_dma_radar.UI.Radar.Views"
             xmlns:vm="clr-namespace:eft_dma_radar.UI.Radar.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <!-- design‑time VM -->
    <d:UserControl.DataContext>
        <d:DesignInstance Type="{x:Type vm:PlayerHistoryViewModel}"
                          IsDesignTimeCreatable="False" />
    </d:UserControl.DataContext>

    <Grid>
        <DataGrid ItemsSource="{Binding Entries}"
                  SelectedItem="{Binding SelectedEntry}"
                  MouseDoubleClick="DataGrid_MouseDoubleClick"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  CanUserSortColumns="False"
                  Margin="4"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Profile.Name}"
                                    Width="Auto"
                                    MinWidth="250" />
                <DataGridTextColumn Header="Account ID"
                                    Binding="{Binding Profile.AccountID}"
                                    Width="Auto"
                                    MinWidth="80" />
                <DataGridTextColumn Header="Acct"
                                    Binding="{Binding Profile.Acct}"
                                    Width="Auto"
                                    MinWidth="50" />
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Profile.PlayerSide}"
                                    Width="Auto"
                                    MinWidth="100" />
                <DataGridTextColumn Header="K/D"
                                    Binding="{Binding Profile.Overall_KD, StringFormat={}{0:N1}}"
                                    Width="Auto"
                                    MinWidth="50" />
                <DataGridTextColumn Header="Total Hours"
                                    Binding="{Binding Profile.Hours}"
                                    Width="Auto"
                                    MinWidth="60" />
                <DataGridTextColumn Header="Raids"
                                    Binding="{Binding Profile.RaidCount}"
                                    Width="Auto"
                                    MinWidth="50" />
                <DataGridTextColumn Header="S/R %"
                                    Binding="{Binding Profile.SurvivedRate, StringFormat={}{0:N1}}"
                                    Width="Auto"
                                    MinWidth="50" />
                <DataGridTextColumn Header="Group"
                                    Binding="{Binding Profile.GroupID}"
                                    Width="Auto"
                                    MinWidth="50" />
                <DataGridTextColumn Header="Alerts"
                                    Binding="{Binding Profile.Alerts}"
                                    Width="Auto"
                                    MinWidth="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
