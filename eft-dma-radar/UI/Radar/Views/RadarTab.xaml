<UserControl x:Class="eft_dma_radar.UI.Radar.Views.RadarTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:eft_dma_radar.UI.Radar.Views"
             xmlns:vm="clr-namespace:eft_dma_radar.UI.Radar.ViewModels"
             xmlns:skia="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <d:UserControl.DataContext>
        <d:DesignInstance Type="{x:Type vm:RadarViewModel}"
                          IsDesignTimeCreatable="False" />
    </d:UserControl.DataContext>
    <Grid>
        <skia:SKGLElement x:Name="Radar"
                          RenderContinuously="True"
                          Panel.ZIndex="0" />
        <local:RadarOverlay x:Name="Overlay"
                            Panel.ZIndex="1" />
        <local:MapSetupHelper x:Name="MapSetupHelper"
                              Panel.ZIndex="2" />
    </Grid>
</UserControl>
