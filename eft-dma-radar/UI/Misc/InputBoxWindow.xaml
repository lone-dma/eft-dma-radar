<Window x:Class="eft_dma_radar.UI.Misc.InputBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:eft_dma_radar.UI.Misc"
        xmlns:vm="clr-namespace:eft_dma_radar.UI.Misc"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=InputTextBox}">
    <d:Window.DataContext>
        <d:DesignInstance Type="{x:Type vm:InputBoxViewModel}"
                      IsDesignTimeCreatable="False"/>
    </d:Window.DataContext>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Prompt -->
        <TextBlock Grid.Row="0"
               Text="{Binding Prompt}"
               VerticalAlignment="Center"
               Margin="5"/>

        <!-- Input -->
        <TextBox Grid.Row="1"
             x:Name="InputTextBox"
             Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
             Margin="5"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="5">
            <Button x:Name="OkButton"
              Content="OK"
              Width="75"
              Margin="5"
              IsDefault="True"
              Command="{Binding OkCommand}"/>
            <Button x:Name="CancelButton"
              Content="Cancel"
              Width="75"
              Margin="5"
              IsCancel="True"
              Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
